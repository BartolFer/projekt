#ifndef VSCODE_ONLY
	#hdr
		#pragma once
		
		record MetaData {
			int width, height;
			int n_channels;
		};
	#end
	#src
	#end
#else
	record MetaData {
		int width, height;
		int n_channels;
	};
#endif
